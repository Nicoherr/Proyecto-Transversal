spring:
  application:
    name: pago-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/db_pago?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:admin2026}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: false
    locations: classpath:db/migration

server:
  port: 8087

logging:
  level:
    root: WARN
    org.springframework.web: DEBUG
    com.marketplace.pago: TRACE
  file:
    name: app.log
    path: ./logs

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

app:
  pedido:
    url: ${PEDIDO_URL:http://localhost:8086}