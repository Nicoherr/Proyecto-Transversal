version: '3.8'

services:

  mysql:
    image: mysql:8.4
    container_name: carrito-mysql
    restart: always
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:dev_carrito}
      MYSQL_USER: ${MYSQL_USER:dev}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:dev123}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:root123}
    ports:
      - "3307:3306"
    volumes:
      - ../docker/mysql/init:/docker-entrypoint-initdb.d
    networks:
      - carrito-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
      interval: 10s

  eurekaserver:
    build:
      context: ../eurekaserver
    image: eurekaserver:1.0
    container_name: carrito-eurekaserver
    ports:
      - "8761:8761"
    networks:
      - carrito-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      timeout: 10s
      retries: 5
      interval: 10s

  carrito:
    build:
      context: .
    image: carrito:1.0
    container_name: carrito-service
    depends_on:
      mysql:
        condition: service_healthy
      eurekaserver:
        condition: service_healthy
    ports:
      - "8081:8081"
    networks:
      - carrito-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      timeout: 10s
      retries: 5
      interval: 10s

networks:
  carrito-network:
    driver: bridge
